alter table stock_adjustment_header add column overall_inventory boolean default 0;
alter table cycle_count_header add column overall_inventory boolean default 0;
alter table cycle_count_details add column  mfg_date date, add column weight double;
alter table cycle_count_details add column system_weight double default 0;
alter table `stock_adjustment_header` add column cycle_count_header_id int, add foreign key(cycle_count_header_id) references cycle_count_header(id) on update cascade;
alter table warehouse add column generate_receiving_upon_dispatch boolean default 0;

ALTER TABLE `stock_adjustment_details` ADD  FOREIGN KEY (`stock_adjustment_header_id`) REFERENCES `stock_adjustment_header`(`id`) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE `stock_adjustment_details` ADD  FOREIGN KEY (`item_id`) REFERENCES `item`(`id`) ON DELETE RESTRICT ON UPDATE CASCADE;
ALTER TABLE `stock_adjustment_details` ADD  FOREIGN KEY (`location_id`) REFERENCES `bin_location`(`id`) ON DELETE RESTRICT ON UPDATE CASCADE;
ALTER TABLE `stock_adjustment_details` ADD  FOREIGN KEY (`uom_id`) REFERENCES `unit_of_measure`(`id`) ON DELETE RESTRICT ON UPDATE CASCADE;
ALTER TABLE `stock_adjustment_details` ADD  FOREIGN KEY (`created_by`) REFERENCES `user`(`id`) ON DELETE RESTRICT ON UPDATE CASCADE;
ALTER TABLE `stock_adjustment_details` ADD  FOREIGN KEY (`updated_by`) REFERENCES `user`(`id`) ON DELETE RESTRICT ON UPDATE CASCADE;


create table cycle_count_scanned_items(
    id int primary key AUTO_INCREMENT,
    cycle_count_header_id int not null,
    location_id int not null,
    item_id int not null,
    lot_number varchar(200),
    expiry_date date,
    mfg_date date,
    uom_id int not null,
    quantity double not null,
    base_quantity double not null,
    weight double,
    barcode_quantity double,
    barcode_uom_id int,
   	barcode_weight double,
    box_number varchar(120) not null, 
    created_date datetime default CURRENT_TIMESTAMP,
    created_by int not null,
    foreign key(location_id) REFERENCES bin_location(id) on update cascade,
    foreign key(item_id) REFERENCES item(id) on update cascade,
    foreign key(uom_id) REFERENCES unit_of_measure(id) on update cascade,
    foreign key(barcode_uom_id) REFERENCES unit_of_measure(id) on update CASCADE,
    foreign key(cycle_count_header_id) REFERENCES cycle_count_header(id) on delete cascade on update CASCADE,
    foreign key(created_by) REFERENCES user(id) on update cascade
);